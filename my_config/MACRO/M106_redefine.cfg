[gcode_macro M106]
rename_existing: M106.1
gcode:
    {% set fan_speed = params.S|default(255)|int %}
    {% if printer.extruder.target >= 240 %}
        # Nad 245°C sniž fan na max 50%
        {% set fan_speed = [fan_speed * 0.3, 76]|min|int %}
    {% endif %}
      M106.1 S{fan_speed}